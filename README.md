## npm registry manager with Rust 🦀

### Usage

```bash
cargo run -- --help

Usage: nrmrs.exe <COMMAND>

Commands:
  ls    List all the registries
  use   Change current registry [use --name <registry_name>]
  help  Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version
```
